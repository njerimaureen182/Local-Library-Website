extends layout

block content
  br
  br
  h2= title
  br
  p Welcome to #[em Local Library], your home of Fantasy, Science Fiction and French Poetry reads.
  br
  h1 Dynamic content
  br
  if error
    p Error getting dynamic content.
  else
    p The library has the following records:

    ul(class="record-list")
      li #[span Books:] !{data.book_count}
      li #[span Copies:] !{data.book_instance_count}
      li #[span Copies available:] !{data.book_instance_available_count} 
      li #[span Authors:] !{data.author_count}
      li #[span Genres:] !{data.genre_count}
  